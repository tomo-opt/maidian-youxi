# Vercel 部署会自动提供 VERCEL_OIDC_TOKEN，无需保存永久密钥。
# 本地调试 AI Gateway 时才需要：
AI_GATEWAY_API_KEY=

# 部署后用于限制允许调用 API 的 GitHub Pages 域名，例如：
# https://your-name.github.io
ALLOWED_ORIGIN=

# 可选模型覆盖
TEXT_MODEL=deepseek/deepseek-v4.1-flash
IMAGE_MODEL=bfl/flux-2-flex
